import React, { useState, useEffect, useRef } from "react";
import ReactDom from "react-dom";

// notification API will be used 

const useNotification = (title, options) => { 
  if (!("Notification" in window))
}

const App = () => {


  return (
      <div className="App">
          <h1>Hi</h1>
      </div>
  )
}
 
export default App; 